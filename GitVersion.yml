mode: ContinuousDeployment

branches:
  main:
    regex: ^main$
    tag: ''
    increment: Patch
    prevent-increment-of-merged-branch-version: true
    is-release-branch: true

  developer:
    regex: ^developer$
    tag: dev
    increment: Minor

ignore:
  sha: []
